SPECIFICATION Spec

CONSTANTS
    (* Constants for EnergyMeter instance EM *)
    EM!Sensors         = {"s1", "s2"}
    EM!Controllers     = {"ctrl1"}
    EM!MaxEvents       = 5

    (* Constants for P2PNetwork instance P2P *)
    P2P!ControllerNode = "ControllerPeer" (* This logically maps to EM!Controllers conceptually *)
    P2P!AuthorityNode  = "AuthorityPeer"
    (* P2P!Nodes is derived from ControllerNode and AuthorityNode in P2PNetwork.tla *)

    (* Constants for Blockchain instance BC *)
    BC!Customers     = {"s1", "s2"} (* Aligning with EM!Sensors *)
    BC!ActionTypes   = {"pauseBilling", "resumeBilling"}

INVARIANTS
    EM!TypeOK
    P2P!TypeOK
    BC!TypeOK

(* Optional: Add properties or view for more detailed checking later *)
(* PROPERTIES *)
(* VIEW *)
